#include<iostream>
#include<string>
using namespace std;
class Student;
class professor;
/*
class Student {
	int id;
public:
	Student(int id) {
		this->id = id;
	}
	friend void show(Student s, professor p);
};
class professor {
	string name;
public:
	professor(string name) {
		this->name = name;
	}
	friend void show(Student s, professor p);
};
void show(Student s, professor p) {
	cout << s.id << p.name;
}
*/